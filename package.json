{
  "name": "wild-west",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "NODE_ENV=production parcel watch index.html && cp -r assets dist",
    "start": "cd dist && python3 -m http.server"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.5.0",
    "typescript": "^4.6.4"
  },
  "dependencies": {
    "phaser": "^3.55.2"
  }
}
